---
version: 1.2
mappings:
- body:
    required: false
    nullable: false
    existsIfEmpty: "$exists($zosAssetResponse.channel.responseCode404)"
    mappings:
    - lastName:
        template: "{{$zosAssetResponse.channel.responseCode404.'lastName'}}"
        required: false
        nullable: false
    - firstName:
        template: "{{$zosAssetResponse.channel.responseCode404.'firstName'}}"
        required: false
        nullable: false
    - telExtension:
        template: "{{$zosAssetResponse.channel.responseCode404.'telExtension'}}"
        required: false
        nullable: false
    - zipCode:
        template: "{{$zosAssetResponse.channel.responseCode404.'zipCode'}}"
        required: false
        nullable: false
    - id:
        template: "{{$zosAssetResponse.channel.responseCode404.'id'}}"
        required: false
        nullable: false
